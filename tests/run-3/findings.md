# Run 3 findings

Written after the run, against the pre-committed declaration. Transcript preserved and name-mapped in `transcript.md`; `verify-output.txt` shows 11 of 11 evidence spans verified against the real workspace before mapping.

## Convergence with the pre-registered hypothesis

Substantial, with one correction. The pre-run hypothesis said the system "has no organ that captures real outcomes." The cold run found that the organ does exist: an `OUTCOME.md` template and a documented `/compound` step that feeds `winning-patterns.md`. It was used once, for the single interview, and never again. So the diagnosis is not "no organ" but a write-back ritual that fires only when there is an obvious external trigger. The Doctor graded it A3 (Expensive Write-Back) manifesting as A1 (Stale Canon), a sharper reading than the builder's.

Primary cause named: A3, Family A. Both critics read `winning-patterns.md` as their calibration; that file has not been touched since the day the system was built, across 53 days and eight application cycles. The internal approval therefore carries no information about real outcomes. 11 of 11 evidence spans verified.

## What the Doctor got weak (method item: assume a mistake)

1. The A3 label emphasizes cost: a ritual that touches many files, or "a session nobody starts." The trace leans almost entirely on the second half, and specifically on the point that negative outcomes have no trigger at all ("nobody's inbox pings when nothing arrives"). That is a real and sharp observation, but it is closer to a trigger gap for one class of fact than to the multi-file expense the mode's name evokes. The label is defensible under A3's own wording, but a reader expecting the cost signature will not find it demonstrated. Recorded as a place where the taxonomy's name and the evidence pull slightly apart.

2. The load-bearing structural claim (only one `OUTCOME.md` exists, for the win, out of eight application folders) rests on the Doctor's hand-census, not on a `verify.py`-checkable span. `verify.py` checks quotes, not file counts or absences. So the single most important fact in the diagnosis, that the loss-side outcomes were never written anywhere, is verified by reading rather than by the mechanical instrument. An instrument limit more than a Doctor error, but it means the strongest claim is the least mechanically checkable.

## Notes

- The Doctor stayed in jurisdiction: it explicitly declined to claim the frozen file is why any real recruiter rejected any package, naming that as a content judgment out of scope.
- The two newest kernel features were exercised and behaved. Step 2b ran and correctly ruled out an altitude mismatch (the structure fits the bounded workflow). The anti-flattening rule held: the Doctor carved out one application that had skipped the critique phase as a separate origin rather than folding it into the primary cause.

## Verdict on the run

Passes as a receipt: cold, memory-clean, 11/11 verified, one primary cause with neighbors ruled out, graded honestly, stopped without prescribing. It converged on the builder's hypothesis and sharpened it. This is the first run on an ordinary, non-meta domain. Anonymized before commit, per the owner's instruction.
